## 1.2.30

- 对象筛选中的“动画窗格”改为调用 WPS 原生 `AnimationCustom` 命令；“图层管理”统一更名为“对象管理”。
- 项目主页改用 WPS 外部链接 API 打开，并在功能区作者信息按钮和侧边栏保留 `designed by Dongsidaye`。
- 对象锁定增加 `WindowSelectionChange` 选中守卫：锁定对象不会被插件定位或保留在画布选区；同时保留 WPS 原生锁定与文档标签回退，并明确报告宿主能力边界。

## 1.2.29

- 侧边栏各功能页统一增加 GitHub 项目主页、当前版本号和作者信息，底部信息条不会遮挡页面内容。
- 版本号统一从插件运行时读取，普通图片清单、智能缩放、图层管理、更新和兼容入口显示一致。

## 1.2.28

- WPS 图层管理改为插件自带任务窗格：按当前普通页、母版或版式列出顶层对象，点击名称/定位可跳转并在正文画布中选中对象。
- 增加逐对象显示/隐藏与锁定/解锁按钮；锁定会先探测 WPS 原生 `Shape.Locked`，当前 WPS 不支持时保存为对象 `Tags` 插件标记并明确提示限制。
- 图层读取限定为当前编辑区域并限制最多 500 个对象，避免打开图层管理时遍历整份演示文稿造成卡顿。

## 1.2.27

- WPS 对象筛选菜单新增专用“选框 + 指针”图标，入口与筛选语义更清晰；安装包会同时携带该图标，避免继续复用图片管理图标。

## 1.2.26

- 修复智能缩放在 30% 以下逐个单元格套用 8pt 下限导致表格标题、正文比例失真的问题；表格文字改用统一缩放因子，最低 4pt 仅作为整表保护。

- 新增 PowerPoint 与 WPS 共用的「筛选」功能区菜单：支持全选、反选、按首个选中对象匹配相同类型/字号/宽度/高度/颜色，以及筛选所有线条、文字和组合对象。
- 对象筛选仅修改原生选区；WPS 增加 JSAPI mock 回归覆盖，无法读取混合字号或颜色时不会改写选区。

## 1.2.25

- 修复 WPS 在缩放文字边距时触发自动适配，导致文字被二次缩小到难以辨认的问题；文字字号现在最后写入，并默认提供 8pt 可读下限，可在面板关闭保护。
- 智能缩放改为带会话编号的“拾取—调整—结束”状态机：结束后旧滑块回调不会重新拾取或修改新选区；连续开启新会话会隔离旧任务，降低 WPS 卡死风险。
- 面板增加“重新拾取选区”和二次确认结束；结束后不会自动复用旧快照，必须明确重新拾取当前选区。
- 为智能缩放制作独立的四角缩放图标，不再复用兼容性诊断图标。

## 1.2.24

- 进一步收紧智能缩放效果兼容策略：WPS 无法确认效果可见状态时跳过数值写入，避免旧版本再次意外开启倒影、阴影、发光或 3D 效果。

## 1.2.23

- 修复智能缩放在 100% 或恢复初始状态时写入关闭的倒影、阴影、发光等效果，导致图片意外出现镜像/特效的问题。
- 智能缩放首次保持 100% 时不再重写几何属性，避免 WPS 对组合对象、裁剪和占位符产生副作用；恢复操作仍会按快照强制还原。

## 1.2.22

- WPS 新增“智能缩放”：支持 1%–300% 实时缩放、中心/四角锚点、目标宽度、初始状态恢复，以及组合对象保持整体缩放。
- 智能缩放与图片清单扫描解耦，选区可包含非图片形状；WPS 不支持的文字/效果属性会逐项跳过，不阻断几何缩放。

## 1.2.21

- 图片清单扫描、缩略图批次、降级导出、分组与重叠检测按 UI 时间预算主动让出 WPS 消息循环，修复点击清单后窗口卡死。
- 缩略图改为最多 4 张一批并逐批写入清单；结构列表先显示，临时演示文稿延后创建。
- 打开清单时不再同步读取外链大图；相同链接内容只渲染一个内嵌代表，源文件状态改为用户执行“更新已修改链接”时检测。
- 定位改为异步等待视图类型和目标页真正生效，再选中并校验正文画布 `Selection.ShapeRange`；移除误导性的右栏“已定位”高亮。
- 新增事件循环最长阻塞与延迟切页后正文图片选中回归测试。

## 1.2.20

- 定位普通页图片时激活文档窗口并校验 `Selection.ShapeRange`，确保高亮落在幻灯片画布中的目标图片，而不是仅标记右侧清单行。
- 图片清单先交付结构化图片列表和定位能力，再后台完成缩略图、指纹和同源分组，降低首次打开的感知等待。
- 缩略图批处理降至 96px；已链接且源文件未修改的图片直接从源文件生成预览，跳过 WPS Copy/Paste 与 Slide.Export。
- 新增结构清单提前交付与画布选中校验回归用例。

## 1.2.19

- 定位普通页图片时先退出母版视图，再跳转到目标页并选中目标形状，修复从母版定位后无法返回普通页的问题。
- 定位成功后，面板实例行显示“已定位”状态，母版、版式和普通页图片分别用明显标签与色带区分。
- 放大图片清单缩略图并优化实例卡片、定位按钮和定位态样式，提升小面板中的可读性。
- Mock 端到端测试新增母版视图退出、普通图片选中高亮回归用例。

## 1.2.14

- 图片清单“替换选中图片”增加不可撤回提醒：首次点击按钮会变红并提示“⚠ 该操作无法撤回，请先保存备份文档！”，4 秒内再次点击确认后才弹出文件选择并执行替换，防止误操作覆盖原图。
- Mock 端到端 58 项全部通过。

## 1.2.13

- 图片清单新增排序/筛选：
  - 默认按“同源应用数量”降序排列（应用最多的排最前面；母版/版式图片按实际应用页数计），每组标题旁显示“应用 N”。
  - 支持切换排序：应用数量 / 名称 / 来源文件。
  - 支持筛选：全部 / 已链接 / 未链接 / 已修改·丢失 / 母版·版式 / 有裁剪；支持按名称或来源文件关键字搜索。
- 替换进度改为“类弹窗式”：批量文件替换、批量剪贴板替换会自动弹出 WPS 居中的进度对话框（官方 Application.ShowDialog 能力），内含进度条、当前第几张/百分比、取消按钮；批量操作在弹窗自身上下文执行，任何 WPS 版本下都能看到实时进度，不受主线程阻塞影响。
- 修复“替换时卡死、看不到进度”的根因：批量替换/更新链接循环中每处理一张图片都会让出事件循环（yield），进度条随每张图实时刷新，不再因为同步桥接调用连跑而看起来卡死。
- 旧版任务窗格进度保留为 ShowDialog 不可用时的自动回退方案。
- Mock 端到端 58 项全部通过；弹窗与排序筛选需在真实 WPS 中验证。

## 1.2.12

- 替换速度优化（重点是图片清单/批量替换）：
  - 有源文件链接的图片在替换时不再走“复制→粘贴到临时稿→清裁剪→量尺寸”的重流程，直接读取源文件头（校验指纹一致后）得到 96dpi 裁剪基准，单张替换的 JSAPI 往返次数大幅下降，已链接图居多的文本提速最明显。
  - 批量替换时对同一新图片只解析一次文件头、只导出一次缩略图/指纹，其余实例直接复用（原实现每张图都重复读文件+导出）。
  - 调整图片图层顺序由“逐次查询当前位置”改为“一次查询+定点下移”，减少每张替换的桥接调用。
  - 图片清单批量渲染 6×6（36 张/次）提升到 8×8（64 张/次），导出次数再降约 40%。
- 新增交互式任务进度：图片清单面板的读取/替换/更新进度条现在显示阶段与张数（如“替换图片 3/42（7%）”），并支持“取消”按钮；功能区/右键菜单触发的批量替换会自动弹出独立“替换进度”窗格（进度条+当前张数+取消），可随时中止并保留已完成部分。
- 批量替换/更新链接/批量同源替换均支持中途取消：取消后返回已完成数量，不丢已替换结果。
- Mock 端到端新增：链接快路径基准校验、进度回调、取消语义；8×8 批渲染适配；共 58 项全部通过。

## 1.2.11

- 修复大尺寸图片替换后裁剪效果被大幅缩小的问题：实测发现 WPS 的裁剪值基准是图片的 96dpi 尺寸（像素 × 0.75pt），而 AddPicture 对超大图片（约 11 英寸以上）返回的“插入尺寸”会被截断，旧版用插入尺寸反推裁剪值，导致 8000px 级图片裁剪被缩小约 7.6 倍。现在替换前会直接解析新图片文件头（PNG/JPEG/GIF/BMP/WEBP）读取真实像素尺寸作为裁剪基准，解析失败时自动回退旧逻辑。
- 图片效果继承增强：除原有的亮度/对比度/颜色类型外，新增反射（Reflection）、发光（Glow）、透明色与透明背景的继承；透明度和饱和度仍受 WPS JSAPI 限制（WPS 的 PictureFormat 未提供通用 Transparency / Saturation 通道），只能尽力继承 WPS 暴露的属性。
- 剪贴板原位替换同步修复：尽力读取剪贴板图片的真实像素尺寸（部分 WPS 内嵌浏览器支持 navigator.clipboard），使大图剪贴板替换也使用 96dpi 裁剪基准，读取失败时回退旧逻辑。
- Mock 端到端测试新增“大图 96dpi 裁剪基准”回归用例与 PNG/JPEG 文件头解析用例，共 51 项全部通过。

# Changelog

## 1.2.10

- 大幅加快「图片清单」首次打开/刷新速度：旧实现每张图片都要走一次「复制→粘贴→单独导出」，每步都要跨 WPS JSAPI 桥。现改为批量渲染——每 36 张图片排成 6×6 网格，放到同一张临时幻灯片上一次导出，再由 JS 解码出每张的缩略图与指纹（感知哈希 + RGBA 指纹），导出次数从 N 次降为约 N/36 次，剪贴板与属性桥接调用也大幅减少；遇到旧版/无 canvas 环境自动回退逐张导出。
- 临时演示文稿改为会话级复用：刷新清单不再反复创建/销毁整份临时文稿。
- 链接状态（源文件哈希）从主流程挪到后台计算：清单先秒出，徽章从「检测中」异步更新为真实状态，源文件很多时不再卡住首屏。
- 新增 flag 门控性能探针（%TEMP%\picture_replace_profile.flag），用于诊断扫描耗时。
# Changelog

## 1.2.9

- 修复功能区出现多个相同图标：WPS 的 getImage 回调控件属性为 Id（大写），旧分发函数按小写 id 匹配全部失效、所有按钮回落到同一张图。现改为每个按钮绑定独立的 getImage 回调（OnGetPanelImage / OnGetFileImage / OnGetFileAllImage / OnGetClipboardImage / OnGetClipboardAllImage / OnGetInfoImage），不再依赖回调参数判断。
- 重新绘制 6 枚互不相同的 32×32 图标：图片清单（相框+山景+替换徽章）、文件原位替换（相框+向上箭头）、批量文件替换（双层相框+替换徽章）、剪贴板原位替换（剪贴板+山景）、批量剪贴板替换（多层纸张+剪贴板）、兼容性诊断（圆形信息 i）。右键菜单同步使用对应图标。
# Changelog

## 1.2.8

- 修复功能区与右键菜单图标显示问号：WPS 的 getImage 只识别插件包内的相对路径（如 "icon.png"），base64 data URL 会被丢弃导致问号。现改为随插件打包 4 枚 32×32 扁平风格图标（图片清单、文件替换、剪贴板替换、兼容性诊断），getImage 按按钮 ID 返回对应图标文件；一键安装包同步纳入图标文件（此前打包清单遗漏，即使路径正确也找不到文件）。
- 修复母版/版式图片「定位」不跳转：母版与版式图片的定位改为切换到母版视图（DocumentWindow.ViewType = ppViewSlideMaster），切换失败时回退跳转到第一个应用页；普通页图片仍直接跳转对应页，并在面板状态栏给出「已切换到母版视图 / 已跳转到第 N 页」反馈。
# Changelog

## 1.2.7

- 图片清单新增「母版 / 版式图片」识别：收集幻灯片母版（SlideMaster）与每个版式（CustomLayouts）里的图片，并计算每张模板图片「应用于哪些页面」（通过 slide.CustomLayout 匹配版式名）。
- 面板中母版图片显示为「母版 · 应用于全部 N 页」，版式图片显示为「版式·<版式名> · 应用于第 X、Y 页」；点击实例定位到第一个应用页。
- 母版/版式图片与普通页图片统一按感知哈希同源分组，可参与同源批量替换（复制/粘贴与插入路径复用现有逻辑）。
- Mock 测试台新增母版/版式收集与应用页断言，共 45 项全部通过。

## 1.2.6

- 「图片清单面板」按钮恢复图标：WPS 不支持 Office 的 imageMso="TaskPane"（显示问号后被移除），现改用 customUI getImage 回调返回内嵌 base64 PNG 图标（叠放照片 + 列表线，32×32），ribbon 大按钮与右键菜单均生效。

## 1.2.5

- 修复图片清单混入文本框/图形：WPS 会为所有形状（文本框、矩形、图表等）暴露 PictureFormat，旧判定把一切形状都当成图片。改为按 shape.Type 判定（13=图片 / 11=链接图片，其余排除；Type 不可用时才回退旧逻辑）。
- 修复「全选同源替换运行到一半报错」：此前误把文本框当图片混入清单与替换目标，替换到非图片对象即失败。Type 判定修复后，清单与替换只处理真图片。
- 右键/选中替换也使用同样的图片判定，不再误操作文本框。
- 读取新图片文件失败时给出明确提示（确认文件存在），不再静默。
- Mock 测试台新增「文本框/图形不进清单、真图片进清单」断言，共 41 项全部通过。

## 1.2.4

- 修复全选同源图替换后部分实例「大小和裁剪变了」的静默降级：WPS 复制/粘贴偶尔失败时，回退的 Duplicate 会返回 1×1 占位尺寸，导致裁剪信息被当作「无裁剪」静默丢弃。现在 1×1 占位视为捕获失败，替换前若检测到「本应有裁剪却读不到」直接跳过该实例并报告原因（原图保持不动），不再产出错误结果。
- 复制/粘贴重试次数 3 → 5。
- 替换与链接更新结果新增失败明细：面板提示「第 X 页「图名」：原因」，最多列 5 条。
- Mock 测试台新增裁剪保持断言（帧尺寸与裁剪值替换后不变）与失败报告断言，共 39 项全部通过。

## 1.2.3

- 同源判定改为感知哈希（dHash，8×8 灰度梯度，汉明距离 ≤ 2 视为同源）：同一张图即使被 WPS/剪贴板重编码成字节不同的文件（实测案例：同视觉两张 PNG 的 md5 完全不同、dHash 距离为 0），也会正确合并为一组；异源图仍严格分离（实测距离 17）。
- 比对对象仍为「未裁剪完整图」：先复制到临时页、清零裁剪、归一化导出后再做感知哈希，裁剪不影响来源判定。
- 分组在任务窗格（CEF）内使用 canvas 解码；若宿主无解码能力自动回退到字节指纹（行为与 v1.2.2 一致）。
- 测试台新增「同视觉不同字节合组 / 异视觉分离」场景，共 35 项断言全部通过。

## 1.2.2

- 修复面板缩略图不显示：缩略图生成从单一 btoa 改为多级回退（btoa → FileReader/Blob → canvas），并新增色块占位兜底，任何 WPS 宿主都能出图。
- 修复同源图被误拆组：分组键去掉宽高比维度，仅按内容指纹分组（同一张图在不同页面被非等比缩放也能正确合并）；宽高比仅作展示信息。
- 面板实例行新增：形状名称小字（便于识别重叠/残留对象）、「重叠」徽章（同页完全重合的两张图互标）、「隐藏」徽章。
- 新增回归测试：无 btoa 环境缩略图回退、同源不同缩放比例合组、重叠检测；测试台共 33 项断言全部通过。

## 1.2.1

- 修复「图片清单面板」空白问题：面板 URL 不再依赖 CurrentWPSAddIn.Path 猜测，改用官方 SDK 的 document.location 推导插件根（http://taskpane.html/），并增加磁盘探测与相对路径兜底；面板内置全局错误显示，任何运行错误会直接显示在面板上。
- 修复「图片清单面板」按钮图标显示为问号：移除 WPS 不支持的 imageMso="TaskPane" 内置图标。
- 测试台新增 addinUrl 三种解析场景断言（共 29 项全部通过）。

## 1.2.0

- 新增「图片清单面板」（对标 InDesign Links 面板）：一键列出全文所有图片，按同源图自动分组，每张实例显示缩略图、页码、页内方位与裁剪标记。
- 自动记住替换源文件（写入形状可持久化字段），面板按组显示链接状态：已链接 / 源文件已修改 / 源文件丢失 / 混合来源 / 无链接。
- 支持组与单实例混合勾选，多选统一原位替换为同一张新图，每处分别保留各自裁剪；替换后自动建立新的源文件链接。
- 支持一键「更新已修改链接」：源文件内容变化时，用源文件原位更新全部实例并刷新指纹；「解除链接」只移除源文件记忆、保留用户自己的无障碍说明。
- 支持双击组名批量重命名（写回形状名称）、点击实例定位到所在页（View.GotoSlide）。
- 剪贴板替换明确不携带链接元数据，避免把剪贴板内容误当作源文件。

## 1.1.7

- 临时演示文稿改为隐藏窗口创建（`Presentations.Add(0)`）并在关闭前标记已保存，批量替换/导出参考图不再闪现新文档窗口、不再弹出“保存/不保存”确认框。
- 剪贴板原位替换改为直接粘贴到目标幻灯片并复用与文件替换完全相同的裁剪保持逻辑（不再经过中间 PNG 导出），裁剪效果不再变化；批量剪贴板替换仍走一次导出，但导出前会把超大图片等比缩放到安全页面尺寸，避免被拉伸。
- 新增右键菜单：在任意图片上右键即可“原位替换图片/批量替换同源图片/剪贴板原位替换/剪贴板批量替换”。


## 1.1.6

- 修复 WPS 批量替换“无法读取参考图片”的根本原因：WPS JSAPI 的 `Shape` 没有 `SaveAsPicture` 方法、`PictureFormat` 没有 `Crop` 子对象。参考图签名与剪贴板图片导出全部改为“临时演示文稿 + `Slide.Export`”方案，裁剪保持只使用文档化的 `CropLeft/Right/Top/Bottom` 标量属性。
- 批量替换现在按“完整原图像素指纹”匹配，不同页面上同一原图的不同裁剪实例均可被识别并分别保留各自的裁剪效果。
- 单张替换不再依赖任何未文档化 API，失败提示区分“未选中图片/无法导出参考图/未找到匹配实例”。


## 1.1.5

- 修复兼容性诊断误报：不再把标题占位符/文本框误当作图片；现在会扫描演示文稿中的真实图片对象后再判断 `PictureFormat.Crop`。
- 当当前文稿没有图片时，诊断会显示“未检测到图片”，不再显示“Crop 不可用”。
- 一键安装包继续自动隔离旧版本并安装新版。

## 1.1.4

- 文件替换改用 WPS 原生 `FileDialog`，点击按钮直接打开系统图片文件管理器。
- 修复部分 WPS 版本任务窗格 URL 解析错误导致的空白面板。
- 一键安装器升级时自动备份并移出旧版插件目录，再安装新版。

## 1.1.2

- 新增 Windows 一键安装器 `PictureReplaceTools-WPS-1.1.2.exe`。
- 安装器写入当前用户 `%APPDATA%\\kingsoft\\wps\\jsaddons`，合并 `publish.xml` 并自动备份原配置。
- 增加可审计的 `install-wps.ps1` / `uninstall-wps.ps1` 安装与卸载脚本。

## 1.1.0

- 新增 PowerPoint 剪贴板原位替换与剪贴板批量替换。
- 新增独立 WPS 演示 JavaScript 加载项源码（`wps_addin/`），遵循 `ribbon.xml + main.js` 与 `wpsjs publish` 部署规范。
- WPS 版支持文件选择、剪贴板 PNG/位图/JPG/GIF 粘贴、裁剪几何恢复和多页同源批量替换。
- 增加 50 MB 文件上限、临时文件清理和失败安全提示。
- 增加 WPS 运行时兼容性诊断，并在批量操作中显示匹配/成功/失败明细。
- 已在本机检测到 WPS WPP 12.1.0.28043 与 COM 宿主；JS 加载项静态构建通过，剪贴板/FileSystem 端到端替换仍需关闭现有用户文档后验收。

## 1.0.0

- 新增“原位替换图片”：保留位置、显示尺寸、旋转、翻转、层级与裁剪焦点。
- 新增“批量替换同图”：扫描当前演示文稿所有幻灯片，按同源图片批量替换。
- 支持不同页面对同一原图设置不同裁剪效果。
- 支持水平/垂直翻转实例识别。
- 使用显式导出缩放和 WIA 像素网格，修复 PowerPoint 导出取整造成的同源图漏检。
- 完成真实 PPTX 回归：3 个共同引用同一媒体部件的实例全部命中，异图保持不变。
